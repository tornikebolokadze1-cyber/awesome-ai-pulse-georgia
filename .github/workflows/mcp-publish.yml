name: MCP npm publish

# Publish @aipulsegeorgia/mcp-server to npm whenever mcp/package.json is
# updated on main. Idempotent — checks if the version already exists on
# npm and skips re-publish when matched.
#
# Auth: npm Trusted Publishing (OIDC). No NPM_TOKEN secret is used — the
# job's id-token is exchanged for a short-lived publish credential, which
# also satisfies npm's 2FA-for-writes requirement and attaches provenance.
# The package's trusted publisher on npmjs.com must point at this repo and
# this workflow file (mcp-publish.yml). Needs npm >= 11.5.1.

on:
  push:
    branches: [main]
    paths:
      - 'mcp/package.json'
  workflow_dispatch: {}

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'
          cache-dependency-path: 'mcp/package-lock.json'

      - name: Update npm (trusted publishing needs npm >= 11.5.1)
        run: npm install -g npm@latest && npm --version

      - name: Install
        working-directory: mcp
        run: npm ci

      - name: Build
        working-directory: mcp
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run build

      - name: Smoke test
        working-directory: mcp
        run: npm run smoke

      - name: Read version
        id: pkg
        working-directory: mcp
        run: |
          VERSION=$(node -p "require('./package.json').version")
          NAME=$(node -p "require('./package.json').name")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "name=$NAME" >> "$GITHUB_OUTPUT"

      - name: Check if already published
        id: check
        run: |
          if npm view "${{ steps.pkg.outputs.name }}@${{ steps.pkg.outputs.version }}" version >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "${{ steps.pkg.outputs.name }}@${{ steps.pkg.outputs.version }} already on npm — skip."
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish
        if: steps.check.outputs.exists == 'false'
        working-directory: mcp
        run: npm publish --access public

      - name: Summary
        run: |
          echo "## MCP publish run" >> "$GITHUB_STEP_SUMMARY"
          echo "- Package: \`${{ steps.pkg.outputs.name }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- Version: \`${{ steps.pkg.outputs.version }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- Already published: \`${{ steps.check.outputs.exists }}\`" >> "$GITHUB_STEP_SUMMARY"
